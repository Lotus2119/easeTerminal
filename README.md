# easeTerminal

A macOS terminal app with a built-in local AI layer. Run commands, hit a shortcut, and let the AI package your terminal context and troubleshoot errors — fully local by default, no API key required.

Built with SwiftUI for Apple Silicon.
